# import inputs validator
import inputs

import numpy as np


def main():
    c, b, A = inputs.getParams()

    # Gx = np.dot(np.dot(np.transpose(x), A), x) + np.dot(np.transpose(b), x) + c


if __name__ == "__main__":
    main()
